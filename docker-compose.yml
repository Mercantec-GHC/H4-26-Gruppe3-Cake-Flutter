services:
  wavelength:
    image: ${DOCKER_REGISTRY-}wavelength:0.2.0
    build:
      context: ./wavelength
      dockerfile: Dockerfile
    container_name: wavelength-flutter
    ports:
      - "${HTTP_PORT}:80"
    restart: unless-stopped
